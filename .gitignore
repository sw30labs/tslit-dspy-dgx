# Python
.venv/
.venv-*/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/

# Secrets / local env
.env
.env.local
*.pem

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local agent instruction files (machine-specific)
AGENTS.md
CLAUDE.md

# Local drafts, not for the public tree
docs/LINKEDIN_POST.md

# Runtime logs (results.tsv / agent_history.jsonl are leftover names from
# the withdrawn Karpathy loop; keep ignored so old files stay untracked)
*.log
run.log
results.tsv
agent_history.jsonl

# Optional large local caches (not used by this port)
.cache/
