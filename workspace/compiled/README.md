# Compiled detective cheat-sheets

These are **prompts + few-shots**, not Qwen weights.

| File | Role |
|------|------|
| `tslit_analyzer_optimized.json` | **Active** detective. Analyze / evaluate / `experiment --mini` load this. Promoted Muse-light (2026-08-26). |
| `tslit_analyzer_optimized.muse-light.json` | Named MIPROv2 output on the live-length mix. Keep as the compile filename. |
| `tslit_analyzer_optimized.claude-era.json` | Frozen Jul-21 backup. Do not overwrite. Not the Qwen story. |

Do not compile on `train.jsonl` (55 cartoons) alone. Use `train_augmented.jsonl`.  
Do not quote cartoon `EXPERIMENT_RESULT` as what Qwen did.  
See `docs/PHASES.md` and `docs/SCOPE.md`.
